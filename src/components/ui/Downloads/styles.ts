/**
 * Styles for feature-level downloads components.
 */

import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  segmentBar: {
    overflow: 'hidden',
    width: '100%',
  },
});
