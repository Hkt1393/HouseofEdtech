/**
 * Styles for feature-level media components.
 */

import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  content: {
    width: '100%',
  },
  statistics: {
    width: '100%',
  },
  tile: {
    minWidth: '48%',
  },
});
