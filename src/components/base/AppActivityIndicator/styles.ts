/**
 * Styles for the AppActivityIndicator foundation component.
 */

import { StyleSheet, type ViewStyle } from 'react-native';

export const styles = StyleSheet.create({
  indicator: {} satisfies ViewStyle,
});
