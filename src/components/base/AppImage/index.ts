/**
 * Public exports for the AppImage foundation component.
 */

export { AppImage } from './container';
export type {
  AppImagePlaceholderSource,
  AppImageProps,
  AppImageSource,
} from './types';
