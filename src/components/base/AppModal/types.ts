/**
 * Public props for the AppModal foundation component.
 */

import type { ModalProps } from 'react-native';

export interface AppModalProps extends ModalProps {}

export interface AppModalViewProps extends AppModalProps {}
