/**
 * Styles for the AppModal foundation component.
 */

import { StyleSheet, type ViewStyle } from 'react-native';

export const styles = StyleSheet.create({
  root: {
    flex: 1,
  } satisfies ViewStyle,
});
