/**
 * Styles for the Section layout primitive.
 */

import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  action: {
    alignSelf: 'flex-start',
  },
  header: {
    alignItems: 'center',
    justifyContent: 'space-between',
  },
  subtitle: {
    flex: 1,
  },
  titleGroup: {
    flex: 1,
  },
});
